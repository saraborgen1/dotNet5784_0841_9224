<Window x:Class="PL.EngineerWorker"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PL"
        mc:Ignorable="d"
        Title="EngineerWorker" Height="450" Width="800"
        DataContext="{Binding RelativeSource={RelativeSource Mode=Self}}">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <TextBlock HorizontalAlignment="Left" Margin="10,0,0,0" TextWrapping="Wrap" Text="{Binding EngineerWorkerProperty}" VerticalAlignment="Top" Height="424" Width="390"/>
        <Button Content="viewList" HorizontalAlignment="Left" Height="97" Margin="34,120,0,0" VerticalAlignment="Top" Width="289" Click="Button_Click" Grid.Column="1"
                 IsEnabled="{Binding EngineerWorkerProperty.Engineer, Mode=OneTime, Converter={StaticResource ConvertIdEngineerIsEnabledKey}}"/>

    </Grid>
</Window>
